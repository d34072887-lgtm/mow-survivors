# 全局规则

## 一、操作记录规则

每次完成一个有意义的操作后，必须将操作摘要追加到当前项目根目录下的 `.action_log.md`：

```
## [日期时间] 操作摘要
- **操作**: 简述做了什么
- **目标**: 涉及的文件/路径
- **状态**: 成功/失败/注意事项
```

**重要**：新增模块或文件后，必须更新索引手册（MEMORY.md 或项目索引文档）。

## 二、网络代理配置

访问 GitHub 等外网资源时使用代理：
```
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

## 三、自动提交规则

代码修改后自动提交：
1. 检查变更：`git status --porcelain`
2. 暂存文件：`git add .`
3. 提交：`git commit -m "类型: 描述"`
4. 拉取：`git pull --rebase`
5. 推送：`git push`

**规范**：类型用中文（新增/修复/优化/重构/文档），描述 ≤ 50 字。

## 四、优秀工作习惯

### 核心原则

1. **模块化开发** - 每个功能独立模块，先跑起来再迭代
2. **基本可运行即成功** - 不追求完美，先让功能跑通
3. **反馈驱动迭代** - 根据实际运行结果迭代优化
4. **不要越权** - 各模块只做自己的事，宁可慢一点也不要责任混乱
5. **索引及时更新** - 开发完成后更新索引手册，方便追溯

### 做事方法

- **遵循第一性原理**：用最快实现的有效方式达成目的
- **不要造轮子**：优先用 GitHub 成熟工具
- **任务拆分**：大目标拆成小目标，每完成一个打钩
- **回复语言**：跟随用户语言

### 代码质量

- **单模块行数控制**：复杂模块拆分成多个简单模块（建议 ≤300 行）
- **session_id 隔离**：需要追踪的操作使用 session_id 隔离数据
- **外置配置**：配置和提示词放到单独文件，方便维护
- **统一入口**：需要集中写入的地方（如记忆区），通过统一入口操作

## 五、编码处理规则

### 基本规范

- **读取**：先检测编码（chardet），再指定 encoding 打开
- **写入**：保持原编码；新建文件用 UTF-8 with BOM
- **终端**：执行 `chcp 65001` 设置为 UTF-8

### 禁止行为

1. ❌ 不检测编码就直接保存
2. ❌ 不确认原编码就转换
3. ❌ 遇到编码错误用 `errors='ignore'`
4. ❌ 覆盖重要文件前不备份

## 六、禁止行为

1. ❌ **禁止造轮子** - 先找 GitHub 成熟方案
2. ❌ **禁止偏离需求** - 不添加用户未要求的功能
3. ❌ **禁止跳过确认** - 重要操作前必须询问

---

**最后更新**: 2026-06-15